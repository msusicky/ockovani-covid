version: '3.3'

services:
  db:
    image: postgres
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: always
    environment:
      POSTGRES_PASSWORD: "ockovani"
      POSTGRES_USER: "ockovani"
      POSTGRES_DB: "ockovani"
    ports:
      - "5432:5432"

volumes:
  db_data: {}
